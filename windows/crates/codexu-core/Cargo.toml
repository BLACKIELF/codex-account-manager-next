[package]
name = "codexu-core"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Core domain models and data readers for codexU Windows port"

[dependencies]
serde.workspace = true
serde_json.workspace = true
chrono.workspace = true
thiserror.workspace = true
anyhow.workspace = true
tokio.workspace = true
tracing.workspace = true
# rusqlite is deferred to phase-2 (Codex provider) because it requires a C compiler on Windows.
# rusqlite = { workspace = true, features = ["bundled", "chrono"] }

[dev-dependencies]
tempfile = "3.10"
